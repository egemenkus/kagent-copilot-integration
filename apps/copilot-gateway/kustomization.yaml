apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: kagent

resources:
  - k8s/rbac.yaml
  - k8s/deployment.yaml

configMapGenerator:
  - name: copilot-gateway-source
    files:
      - proxy_server.py=src/proxy_server.py
