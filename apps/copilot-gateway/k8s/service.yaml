apiVersion: v1
kind: Service
metadata:
  name: copilot-gateway-svc
  namespace: kagent
  labels:
    app.kubernetes.io/name: copilot-gateway
spec:
  ports:
  - port: 8080
    targetPort: 8080
    name: http
  selector:
    app.kubernetes.io/name: copilot-gateway
